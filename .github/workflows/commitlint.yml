name: Commitlint

on: [pull_request]

jobs:
  commitlint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Lint PR title
        uses: marocchino/commitlint-action@v5
        with:
          configFile: .commitlintrc.json
